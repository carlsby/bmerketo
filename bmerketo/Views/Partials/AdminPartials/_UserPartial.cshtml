@model UserViewModel

@{
    var role = await userService.GetAllRolesAsync();
}

<tbody>
    <tr>
    <form method="post">
@*                <th class="col text-white drop-down dropdown-toggle-split" scope="row">
            <input type="hidden" asp-for="@Model.Id" />
            <select class="user-select" asp-for="@Model.UserRoles">
                @foreach (var roles in role)
                {
                    <option class="text-dark"value="@roles.Name">@roles.Name</option>
                }
            </select>
        </th>*@

        <input type="hidden" asp-for="@Model.Id" />
        <td class="col text-white" value="@Model.UserRoles">@Model.UserRoles</td>
        <td class="col text-white">@Model.FirstName</td>
        <td class="col text-white">@Model.LastName</td>
        <td class="col text-white">@Model.Email</td>
        <td class="col text-white"><i type="button" data-bs-toggle="modal" data-bs-target="#exampleModal" class="fa-solid fa-user-pen"></i></td>
@*                <button type="submit" class="btn-admin mb-2" id="save-btn @Model.FirstName">SAVE CHANGES</button>*@
        </form>


        <!-- Modal -->
        <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h1 class="modal-title fs-5" id="exampleModalLabel">Modal title</h1>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        ...
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-primary">Save changes</button>
                    </div>
                </div>
            </div>
        </div>
    </tr>
</tbody>



<script>

    function ShowButton() {
        var btn = document.getElementById('save-btn');
        btn.style.display = "block";
        btn.style.margin = "0 auto";
    }

</script>